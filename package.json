{
  "name": "env-time-travel",
  "version": "0.3.0",
  "description": "CLI for tracing .env variable history through git",
  "type": "module",
  "bin": {
    "env-time-travel": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "rm -rf dist && tsc",
    "test": "node --import tsx/esm --test test/**/*.test.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0"
  },
  "engines": {
    "node": ">=25"
  }
}
